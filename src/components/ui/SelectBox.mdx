---
name: SelectBox
---

import { Playground, PropsTable } from 'docz'
import SelectBox from './'
const itemsArray = [
  {
    id: 2,
    value: "registered",
    selected: true
  },
  {
    id: 3,
    value: "portfolio-building",
    selected: true
  },
  {
    id: 4,
    value: "portfolio-review",
    selected: true
  },
  {
    id: 5,
    value: "social-media-screen",
    selected: true
  },
  {
    id: 6,
    value: "react-shortlist",
    selected: false
  }
]
const promisedAttr = (label, value, timer = 3000) => new Promise(resolve =>
  setTimeout(() => {
    console.log(label, value)
    resolve(value)
  }, timer)
)

# SelectBox

<PropsTable of={SelectBox} />

## Simple view for Add to List button

<Playground>
  <SelectBox
    collectionName="List"
    items={itemsArray}
    width={300}
    itemsHeight={3}
    onSelect={item => promisedAttr('onSelect', item)}
    onCreateNew={listName => promisedAttr('onCreateNew', {
      id: new Date().getUTCMilliseconds(),
      value: listName
    })}
  />
</Playground>
