Card view mode:

```js
<SearchForm
  mode="card"
  onSubmit={() => console.log('onSubmit')}
>
</SearchForm>
```

Tabular view mode:

```js
<SearchForm
  mode="tabular"
  onSubmit={() => console.log('onSubmit')}
>
</SearchForm>
```

Missing props (does component explode?):

```js
<SearchForm />
```
