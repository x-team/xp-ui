// @flow

import React from 'react'
import { storiesOf } from '@storybook/react'

import Typography from './Typography'

storiesOf('UI Components/Typography', module)
  .add('typography sets for development reference', () => <Typography />)
